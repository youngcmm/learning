//sort函数的使用
#include<stdio.h>
#include<algorithm>
#include<iostream>
#include<vector>
using namespace std;
//sort(首元素地址，尾元素地址的下一个地址，比较函数)前两个必填写，若不填写比较函数则默认增序，比较函数由自己定义。
struct node
{
    int x,y;
}n[3];
//bool cmp(int a,int b)//整型，若容器为整型也用整型
//{
//    return a>b;
//}
//bool cmp(char a,char b)
//{
//    return a>b;
//}
//bool cmp(double a,double b)
//{
//    return a>b;
//}
//bool cmp(node a,node b)//自定义结构体排序
//{
//    return a.x>b.x;
//}
//bool cmp(node a,node b)//自定义结构体先按x排序若x相同则按y排序
//{
//    if(a.x!= b.x ) return a.x >b.x;
//    else return a.y > b.y;
//}
//bool cmp(string s1,string s2)//以字符串的长度排序
//{
//    return s1.length() > s2.length();
//}
int main()
{
//    int a[6] = {9,4,2,5,6,-1};
//    char b[6]={'c','q','m','k'};
//    double c[6] = {1.1 , 3.2 , 4.1 ,5.1,8,9};
//    n[0].x = 1;
//    n[0].y = 2;
//    n[1].x = 1;
//    n[1].y = 4;
//    n[2].x = 5;
//    n[2].y = 6;
//    sort(a,a+5);//从a[0]到a[4]
//    sort(b,b+6);
//    sort(c,c+6);
//    for(int i = 0;i<6;i++)
//    printf("%d ",a[i]);
//    printf("\n");
//    for(int i = 0;i<6;i++)
//    printf("%c ",b[i]);
//    printf("\n");
//    for(int i = 0;i<6;i++)
//    printf("%.1lf ",c[i]);
//    return 0;
    //用cmp函数实现
    //sort(a,a+5,cmp);//从a[0]到a[4]
 //   sort(b,b+6,cmp);
 //   sort(c,c+6,cmp);
//    sort(n,n+3,cmp);
//    for(int i = 0;i<6;i++)
//    printf("%d ",a[i]);
//    printf("\n");
//    for(int i = 0;i<6;i++)
//    printf("%c ",b[i]);
//    printf("\n");
//    for(int i = 0;i<6;i++)
//    printf("%.1lf ",c[i]);
//    printf("\n");
//    for(int i = 0;i<3;i++)
//    {
//    printf("%d %d",n[i].x,n[i].y);
//    printf("\n");
//    }
    //容器的排序
//    vector<int>vi;
//    vi.push_back(3);
//    vi.push_back(1);
//    vi.push_back(2);
//    sort(vi.begin(),vi.end(),cmp);
//    for(int i = 0;i< 3;i++)
//    {
//        printf("%d ",vi[i]);
//    }
//    printf("\n");
    //对string排序
    string str[3]={"cc","a","bbb"};
    sort(str,str+3,cmp);
    for(int i = 0;i< 3;i++){
    cout<<str[i]<<endl;
    }    //字符串数组不能用printf打印，因为string类型是c++内置函数不能用printf打印,printf只能打印字符串（字符数组）！！
    return 0;
}
